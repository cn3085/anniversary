export default function quizReducer(state, action){
    switch (action.type) {
        case '':
            return {
                
            }
        default:
            throw new Error()
    }
}